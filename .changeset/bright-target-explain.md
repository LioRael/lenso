---
"@lenso/cli": patch
---

Add `lenso app explain --json` for persisted Host target-admission and
Capability-binding evidence, plus `lenso engine explain` and per-run execution
cache/Generation explanations. Both commands report real owned evidence rather
than accepting a manually supplied target-profile claim.
