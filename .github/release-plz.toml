[workspace]
# The dispatch-only release gate is the versioning boundary. It verifies an
# exact landed SHA, candidate CI evidence, and the complete registry-derived
# package/version set before release-plz can publish opted-in crates.
release_always = true
changelog_update = false
git_tag_name = "{{ package }}-v{{ version }}"
