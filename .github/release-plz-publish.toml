[workspace]
# The repository default is release_always = false so only release-plz PR
# merges publish. This config is used only by the release job's recovery
# phases, including squash-merge recovery commits that are not associated
# with the release PR in the GitHub API.
release_always = true
changelog_update = false
git_tag_name = "{{ package }}@{{ version }}"
