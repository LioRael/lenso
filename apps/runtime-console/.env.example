# Runtime Console mode:
# - mock keeps the UI fully local with seeded data.
# - api is reserved for the next backend wiring slice.
VITE_RUNTIME_CONSOLE_MODE=mock

# Reserved for the future API client foundation. Mock mode remains default.
VITE_API_BASE_URL=http://localhost:3000
