# Event Contracts

Event payload JSON Schemas live here, grouped by owning domain.

Event names are versioned:

- `identity.user_registered.v1`
- `files.object_uploaded.v1`
- `notifications.message_sent.v1`

The platform event envelope is shared; only payload schemas vary by event.

