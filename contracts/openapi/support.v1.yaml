openapi: 3.1.0
info:
  title: Support Service Contract
  version: 1.0.0
paths:
  /tickets:
    get:
      operationId: listTickets
      responses:
        "200":
          description: Tickets returned
