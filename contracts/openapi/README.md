# OpenAPI Contracts

Generated OpenAPI 3.1 documents live here.

Initial targets:

- `app-api.v1.yaml`
- per-domain fragments under `modules/` if needed later

Source of truth is Rust route and DTO metadata. Generated files are committed so CI can diff contract changes.

