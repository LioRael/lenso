//! Runtime Story module.
//!
//! This module owns the Story Runtime Console module manifest. The compatible
//! `/admin/runtime/*` backend remains mounted through `platform-admin` while
//! the Story backend is extracted in slices.

pub mod module;
