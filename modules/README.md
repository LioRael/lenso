# Application Modules

The framework does not bundle concrete application or Console modules.

Linked modules belong to the host application repository that composes them.
The Lenso Console Service owns its Runtime Story module in the sibling
`lenso-console` repository.
