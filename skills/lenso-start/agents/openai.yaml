interface:
  display_name: "Lenso Start"
  short_description: "Route Core, Web, or Agent work to one owner."
  default_prompt: "Use $lenso-start to choose the Core, Web, or Agent task map, then route this request to one primary Lenso owner workflow."
policy:
  allow_implicit_invocation: false
