interface:
  display_name: "Lenso Runtime Extension"
  short_description: "Extend Engine processing or Host execution."
  default_prompt: "Use $lenso-runtime-extension to implement this Engine processing, embedding or Host execution change in its owning layer, with real consumer proof and portable Kernel boundaries preserved."
