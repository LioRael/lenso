interface:
  display_name: "Lenso Runtime Extension"
  short_description: "Extend Plugin execution and App Generations."
  default_prompt: "Use $lenso-runtime-extension to implement this Driver, Execution Adapter, Runner, Plugin control-plane, App Generation, language-process/wire, or endpoint change without moving product behavior into Kernel."
